<Page
    x:Class="Zink.Pages.LinkedInPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Zink.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Spacing="12" Padding="12"
                    Background="{ThemeResource LayerFillColorDefaultBrush}">
            <TextBlock Text="LinkedIn" FontSize="20" FontWeight="SemiBold"/>
            <ProgressRing x:Name="LoadingRing" IsActive="True" Width="20" Height="20" Margin="8,0,0,0"/>
        </StackPanel>
        <WebView2 x:Name="MyWebView" Grid.Row="1"/>
    </Grid>
</Page>
